@inject IDbContextFactory<MyCsustanContext> DbFactory
@inject DialogService DialogService

<RadzenTemplateForm Data="@_model" TItem="PortalConfig" Submit="OnSubmit">
    <RadzenFieldset Text="Portal Config">
        <RadzenStack Gap="1rem">
            <RadzenDropDown Data="_baseUrls" TextProperty="BaseUrlName" ValueProperty="BaseUrlId" @bind-Value="_model.PeopleSoftCsBaseUrlId" Placeholder="PeopleSoft CS Base URL" Style="width: 100%" />
            <RadzenDropDown Data="_baseUrls" TextProperty="BaseUrlName" ValueProperty="BaseUrlId" @bind-Value="_model.PeopleSoftHrBaseUrlId" Placeholder="PeopleSoft HR Base URL" Style="width: 100%" />
            <RadzenDropDown Data="_baseUrls" TextProperty="BaseUrlName" ValueProperty="BaseUrlId" @bind-Value="_model.CashetBaseUrlId" Placeholder="CashNet Base URL" Style="width: 100%" />
            <RadzenDropDown Data="_baseUrls" TextProperty="BaseUrlName" ValueProperty="BaseUrlId" @bind-Value="_model.CampusWebsiteBaseUrlId" Placeholder="Campus Website Base URL" Style="width: 100%" />
            <RadzenDropDown Data="_baseUrls" TextProperty="BaseUrlName" ValueProperty="BaseUrlId" @bind-Value="_model.DrawBaseUrlId" Placeholder="Draw Base URL" Style="width: 100%" AllowClear="true" />
            <RadzenDropDown Data="_linkGroups" TextProperty="PortalLinkGroupName" ValueProperty="PortalLinkGroupId" @bind-Value="_model.CovidScreeningPortalLinkGroupId" Placeholder="Covid Screening Link Group" Style="width: 100%" AllowClear="true" />
            <RadzenTextBox @bind-Value="_model.MaskedEmplid" Name="MaskedEmplid" Placeholder="Masked Emplid" Style="width: 100%" />
            <RadzenTextBox @bind-Value="_model.MaskedSsn" Name="MaskedSsn" Placeholder="Masked SSN" Style="width: 100%" />
        </RadzenStack>
    </RadzenFieldset>

    <RadzenStack Orientation="Orientation.Horizontal" JustifyContent="JustifyContent.SpaceBetween" class="mt-3">
        <RadzenButton Text="Delete" ButtonStyle="ButtonStyle.Danger" Click="OnDelete" Disabled="@(_model.PortalConfigId == 0)" />
        <RadzenStack Orientation="Orientation.Horizontal" Gap="0.5rem">
            <RadzenButton Text="Save" ButtonStyle="ButtonStyle.Primary" Type="ButtonType.Submit" />
            <RadzenButton Text="Cancel" ButtonStyle="ButtonStyle.Light" Click="() => DialogService.Close(false)" />
        </RadzenStack>
    </RadzenStack>
</RadzenTemplateForm>

@code {
    [Parameter] public int? PortalConfigId { get; set; }

    private PortalConfig _model = new();
    private List<BaseUrl> _baseUrls = new();
    private List<PortalLinkGroup> _linkGroups = new();

    protected override async Task OnInitializedAsync()
    {
        await using var db = await DbFactory.CreateDbContextAsync();
        _baseUrls = await db.BaseUrls.AsNoTracking().OrderBy(b => b.BaseUrlName).ToListAsync();
        _linkGroups = await db.PortalLinkGroups.AsNoTracking().OrderBy(g => g.PortalLinkGroupName).ToListAsync();

        if (PortalConfigId is null)
        {
            return;
        }

        var entity = await db.PortalConfigs.FindAsync(PortalConfigId.Value);
        if (entity is not null)
        {
            _model = entity;
        }
    }

    private async Task OnSubmit(PortalConfig model)
    {
        await using var db = await DbFactory.CreateDbContextAsync();

        if (model.PortalConfigId == 0)
        {
            db.PortalConfigs.Add(model);
        }
        else
        {
            db.PortalConfigs.Update(model);
        }

        await db.SaveChangesAsync();
        DialogService.Close(true);
    }

    private async Task OnDelete()
    {
        var confirm = await DialogService.Confirm("Delete this portal config?", "Confirm Delete");
        if (confirm != true)
        {
            return;
        }

        await using var db = await DbFactory.CreateDbContextAsync();
        var entity = await db.PortalConfigs.FindAsync(_model.PortalConfigId);
        if (entity is not null)
        {
            db.PortalConfigs.Remove(entity);
            await db.SaveChangesAsync();
        }

        DialogService.Close(true);
    }
}
